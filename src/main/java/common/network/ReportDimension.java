package common.network;

import java.io.Serializable;

/** Supported grouping dimensions for the principal's statistical reports. */
public enum ReportDimension implements Serializable {
    TEACHER,
    COURSE,
    STUDENT
}
